version: "3.8"

services:
  proxy:
    build: .
    container_name: open-proxy
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    # Optional: add these labels so CasaOS shows it nicely in the dashboard
    labels:
      icon: "🌐"
